package api4debtus

import (
	"github.com/sneat-co/sneat-go-core/module"
)

// RegisterHttpRoutes initiates users module
func RegisterHttpRoutes(handle module.HTTPHandleFunc) {
	// TODO move HTTP handlers related to here
}
